# 通用参数转换工具 设计文档 (design.md)

> 项目名：CommonParametersHelper / ContainerTemplateManager
> 版本：v1.0 设计稿
> 用途：本文档是给开发（含 AI 辅助开发）的实现依据，描述**做什么**与**为什么**，不规定具体代码写法。

---

## 1. 项目背景与目标

### 1.1 背景

设备（AGV / 叉车机器人）的全部配置以「通用参数」（下称**通参**）JSON 文件承载。当设备软件升级到新版本时，新版本会附带一份**新的通参模板**，其中：

- 参数的**结构**可能变化（新增、删除、重命名、移位）
- 参数的**默认值**可能被官方调优
- 参数的**校验规则、数据类型、单位**可能变更

而设备上正在运行的**旧通参**里，保存着这台设备的实际配置，包括：

- 传感器标定值（雷达外参、货叉高度等）——与**物理硬件强绑定**，是这台设备独有的资产
- 现场为适配特殊场景做的人工微调

升级时若直接使用新模板，这些设备特有的值会全部丢失，设备将无法正确运行。

### 1.2 目标

开发一个带 GUI 的桌面工具，输入**旧通参 JSON** 与**新通参 JSON**，自动输出一份**结构符合新版本、取值保留设备特性**的新通参 JSON，可直接导入设备使用。

### 1.3 核心设计原则

| 原则 | 说明 |
| --- | --- |
| **全自动优先** | 一键转换模式下不得因任何情况阻塞或崩溃，必须有兜底策略 |
| **数据不丢失** | 旧通参中的参数不允许被静默丢弃 |
| **结构自洽** | 输出文件必须满足全部结构不变量，否则拒绝导出 |
| **格式保真** | 输出文件格式必须与新通参完全一致，见 §6A |
| **可追溯** | 每个参数的取值决策都要在报告中说明来源与原因 |
| **逻辑与 UI 解耦** | 转换引擎为纯逻辑模块，可独立测试与命令行调用 |

---

## 2. 数据结构规格

### 2.1 文件顶层结构

```json
{
  "HashCode": -1116662397,
  "Data": [ /* 扁平化节点数组 */ ],
  "Tags": [ { "TagName": "新增" }, ... ]
}
```

- `HashCode`：整数（有符号 32 位）。**经确认设备导入时不做校验**，处理方式见 §7.6。
- `Data`：**扁平数组**，非嵌套。树形关系通过 `ParentUUID` 表达。
- `Tags`：标签字典。**注意：节点中出现的 `TagId` 取值范围（2~13）与本数组长度（10）不一致，不可用下标对应。**

### 2.2 节点字段规格

以样本文件（1878 节点）实测为准。

#### 结构与定位字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `UUID` | string | 节点唯一标识。**文件内唯一，但跨文件随机生成，不具备跨文件身份意义** |
| `ParentUUID` | string | 父节点 UUID，根节点为空字符串 `""` |
| `NodeType` | int | `0` = 叶子节点（真实参数，1696 个）；`1` = 分组节点（无值，182 个） |
| `Level` | int | 层级，根节点为 1，取值 1~5 |
| `NodeCode` | int | 同级排序码 |
| `Path` | string | 完整路径，如 `config/3dSlam/LidarLocChangeMap/ChangeMapAngleThreshold` |
| `Id` | int | 平台数据库自增 ID，与 UUID 并存 |

#### 展示字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `NodeName` | string | 中文显示名。**存在重名**（如「激光雷达外参相对位置」出现多次），不可单独作为主键 |
| `SystemName` | string | 系统内部名（英文），构成 `Path` 的最后一段 |
| `ParameterDestriottion` | string | 参数说明。**注意原文拼写有误**（应为 Description），实现时必须照抄该错误拼写 |

#### 取值与校验字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `DataTypeId` | string | 数据类型，见 §2.4 |
| `ParameterUnit` | string | 单位，如 `米(m)`、`弧度(rad)`、`无(-)` |
| `ValueRange` | string | 人类可读的范围描述，如 `≥0`、`>= 0 && <= 1` |
| `CheckType` | int | 校验类型，见 §2.5 |
| `VerifyExpression` | string | **字符串化的 JSON**，格式随 CheckType 变化，需二次解析 |
| `ParameterValue` | string | 当前值。**所有类型一律以字符串存储** |
| `ParameterDefaultValue` | string \| null | 默认值。分组节点为 `null` |

#### 其他字段

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `IsEdit` / `IsShow` | bool | 是否可编辑 / 展示 |
| `TagId` / `Tag` / `TagName` | int / string / string | 单标签分类。样本中一个节点最多一个标签 |
| `ModifyTime` | string | ISO 8601 时间戳 |
| `ChildNodes` | array | **样本中恒为空数组**，不承载真实嵌套关系 |
| `IsSearchExportNode` | bool | 样本中恒为 `false`，用途未知，原样保留 |
| `RangeExplain` | null | 样本中恒为 `null`，用途未知，原样保留 |
| `LstEnumExplain` | array | 样本中恒为空数组，原样保留 |

### 2.3 结构不变量（已实测验证，零例外）

以下四条在样本文件中 **1878/1878 全部成立**，必须作为输出文件的硬性校验条件：

1. 每个非根节点的 `ParentUUID` 都能在本文件中找到对应节点（**无孤儿节点**）
2. `Path` == `父节点.Path + "/" + SystemName`；根节点为 `"config/" + SystemName`
3. `Level` == `父节点.Level + 1`；根节点 `Level` == 1
4. `UUID`、`Id`、`Path` 三者在文件内均**全局唯一**

### 2.4 DataTypeId 取值表

| 值 | 含义 | 样本数量 | 示例 |
| --- | --- | --- | --- |
| `""` | 分组节点占位 | 182 | — |
| `"1"` | 布尔 | 236 | `true` / `false` |
| `"6"` | 整数 | 162 | `1` |
| `"10"` | 浮点（无单位） | 427 | `0.304000` |
| `"11"` | 浮点（带单位） | 680 | `0.1` |
| `"12"` | 字符串枚举 | 191 | `Ver2` |

**特殊情况**：部分参数（如雷达外参）的值为**逗号分隔的多值字符串**，如 `2.081228,-0.010348,2.054715`，`DataTypeId` 仍标为 `10`。校验时需按分量逐个处理，或在无法解析时跳过校验。

### 2.5 CheckType 与 VerifyExpression 规格

| CheckType | 含义 | 样本数量 | VerifyExpression 格式 |
| --- | --- | --- | --- |
| `0` | 不校验 | 736 | `""` 或 `"-"` |
| `2` | 区间校验 | 868 | JSON：`{"Min":x,"MinOperator":n,"Max":y,"MaxOperator":m}` |
| `3` | 枚举校验 | 274 | JSON 数组：`[{"Code":"true","Describe":"true"}, ...]` |

#### 运算符编码表（通过 `ValueRange` 反推验证）

| 编码 | 所在字段 | 语义 | 样本数 | 反推依据 |
| --- | --- | --- | --- | --- |
| `2` | `MinOperator` | `Min ≤ 值` | 345 | `{"Min":0,"MinOperator":2,"Max":1,"MaxOperator":2}` ↔ `>= 0 && <= 1` |
| `2` | `MaxOperator` | `值 ≤ Max` | 345 | 同上 |
| `3` | `MaxOperator` | `值 > Max` | 39 | `{"Max":0,"MaxOperator":3}` ↔ `>0` |
| `4` | `MaxOperator` | `值 ≥ Max` | 484 | `{"Max":0,"MaxOperator":4}` ↔ `≥0` |

> ⚠️ **关键陷阱**：编码 `3` 与 `4` 出现在 `Max` 字段中，但语义是**下界**。这是平台的数据录入习惯——单边约束时一律填入 `Max` 槽位，方向由运算符决定。若按字面理解为「上界」，`≥0` 将被错解为 `≤0`，导致**所有非负参数校验失败**。

> ⚠️ **未验证编码**：样本中 `MinOperator` **仅出现过编码 2**，其他编码语义无法确认。运算符映射必须实现为**配置表**（非硬编码），遇到未知编码时按「跳过校验 + 记录告警」处理。

#### 数据质量异常（必须容错）

- **53 个节点 `CheckType=0` 却携带非空表达式**，且内容是人类可读文本（如 `>= -10 && <= 10`）而非 JSON。
  → 解析必须 try/except 包裹，失败时按「不校验」降级，**严禁抛异常中断流程**。
- `ValueRange` 中的比较符存在**全角/半角混用**（`≥` 与 `>=`、`＞` 与 `>`），若用于交叉校验需先归一化。

---

## 3. 匹配策略

### 3.1 主键选择

**以 `Path` 作为跨文件匹配主键。**

实测依据：样本文件中 1878 个节点的 `Path` **完全唯一**，无重复。

**明确排除的方案**：

- ❌ `UUID` —— 跨文件随机生成，无身份意义，**任何情况下都不得用于跨文件匹配**
- ❌ `Id` —— 平台数据库自增，跨版本不保证一致
- ❌ `NodeName` 单独使用 —— 存在重名

**`NodeName` 的正确用途**：作为**一致性校验**。当 `Path` 匹配成功但 `NodeName` 不一致时，说明参数被改名，应记入报告告警（不阻断流程）。

### 3.2 匹配流程

```
对旧通参的每个叶子节点（NodeType == 0）：

  ① 强制映射查表
     若该节点 Path 命中强制迁移清单中配置的「新版本路径」映射
     → 直接按映射结果匹配，跳过后续步骤

  ② Path 精确匹配
     在新通参中查找相同 Path 的节点
     → 命中：进入取值决策
        ├─ 附带检查 NodeName 是否一致，不一致则记录告警
        └─ 附带检查 DataTypeId / ParameterUnit 是否变更，变更则记录告警

  ③ 重命名嗅探（Path 未命中时）
     依次尝试：
     a. 父 Path 相同 + NodeName 相同
     b. NodeName + DataTypeId + ParameterUnit 三者完全相同（全局搜索）
     → 命中：判定为「重命名/移位」，按匹配处理，报告中标注
     → 多个候选：取第一个并在报告中列出全部候选，标注为低置信匹配

  ④ 判定为「新通参中不存在」
     → 进入 P4 节点补建流程
```

### 3.3 为什么需要重命名嗅探

若跳过步骤 ③，直接把未匹配的旧参数补建到新文件中，会产生严重后果：

**场景**：新版本将 `LidarGridVoxel` 重命名为 `LidarVoxelSize`。

- Path 匹配失败 → 补建旧的 `LidarGridVoxel`
- 结果：新文件中**同时存在**新旧两个语义相同的参数
- 设备读取哪一个都可能出错，且**不会报任何错误**

---

## 4. 取值决策引擎（核心）

### 4.1 决策优先级

**从上往下逐条判断，第一条命中即停止。**

| 优先级 | 条件 | 动作 | 报告分类 |
| --- | --- | --- | --- |
| **P0** | 命中**强制迁移清单**（标定类 / 静态类） | **无条件**将旧 `ParameterValue` 迁移至新节点的 `ParameterValue`，忽略新默认值 | 强制迁移 |
| **P1** | 旧 `ParameterValue` ≠ 旧 `ParameterDefaultValue` | 将旧 `ParameterValue` 迁移至新节点的 `ParameterValue` | 人工微调迁移 |
| **P2** | 旧 `ParameterValue` == 旧 `ParameterDefaultValue` | 使用新模板的默认值与当前值，不做修改 | 采用新默认 |
| **P3** | 新通参有、旧通参无（新版本新增参数） | 使用新模板的默认值与当前值 | 新版本新增 |
| **P4** | 旧通参有、新通参无（且重命名嗅探未命中） | 补建节点，见 §5 | 补建迁移 |

**统一约束**：无论走哪条路径，新节点的 `ParameterDefaultValue` **一律采用新模板的默认值**，不从旧文件迁移。默认值属于软件版本的属性，不属于设备的属性。

**分组节点（`NodeType == 1`）不参与取值决策**，仅参与结构匹配与补建。

### 4.2 P0 为什么必须凌驾于 P1/P2 之上

标定值与静态参数描述的是**这台设备的物理事实**，而非用户偏好。

**反例说明**：`config/static/Accessory/ForkLength`（货叉长度）当前值 `1.49`，默认值也是 `1.49`。

- 按 P2 规则（当前 == 默认）→ 采用新模板默认值
- 若新版本将默认改为 `1.2` → **这台车的货叉长度凭空缩短 29 厘米**

**值是否等于默认值是巧合，不代表该参数不重要。** 因此这类参数必须无条件迁移。

### 4.3 强制迁移清单为什么不能靠 Tag 自动推导

已实测验证 `TagName` 字段**不足以**识别需强制迁移的参数：

- 41 个被定制过的参数中，**19 个没有任何 Tag**（如「传感器触地临界点的货叉高度」、`ForkCMax`、`ForkCMin`）
- `config/static` 子树的 128 个叶子节点中，有 Tag 与无 Tag 的混杂存在
- 反向看，`可变-生产维护` 标签下的 59 个叶子中，仅 17 个被实际定制过

**结论**：必须依赖人工梳理的外部清单。`TagName` 可作为报告中的辅助分组信息，但不作为判定依据。

### 4.4 校验失败的兜底策略

迁移后的值必须通过**新版本**的校验规则（`DataTypeId` + `CheckType` + `VerifyExpression`）。

**统一策略**：

> 任何迁移值未通过新版本校验时，**一律回退使用新模板的默认值**，保证程序不中断；同时在报告中以**最高级别**标出，注明「需人工二次复核」。

**报告呈现要求**：

- P0 白名单参数触发回退时，**在报告中单独成组并置顶显示**
  理由：标定值被回退意味着设备物理参数被改写，必须有人确认
- 报告需同时展示：旧值、新默认值、失败的校验规则、失败原因

**同时必须作为独立告警项列出的变更**（即使值校验通过）：

- `ParameterUnit` 变更 —— 最危险。单位从 `弧度(rad)` 改为 `度(°)` 时，数值合法但物理含义完全错误，**不会触发任何校验错误**
- `DataTypeId` 变更 —— 如 `6`（整数）→ `11`（浮点），或反向导致精度丢失
- `CheckType` / `VerifyExpression` 变更 —— 校验规则收紧或放宽

---

## 5. 节点补建规则（P4）

将旧通参中新模板不存在的节点补建进输出文件时，**不得直接复制旧节点**。以下字段必须重新生成：

| 字段 | 处理方式 |
| --- | --- |
| `UUID` | **重新生成**新的 UUID。旧 UUID 在新文件中无意义，直接沿用会破坏结构自洽 |
| `ParentUUID` | 指向**新文件中**对应父节点的 UUID。父节点通过 **Path 查找**，不得使用旧文件的 ParentUUID |
| `Id` | 取新文件当前 `max(Id) + 1` 递增分配，避免与新模板 Id 冲突 |
| `Level` | 按 Path 深度重算（`config/a/b` → Level 2） |
| `NodeCode` | 取同级节点 `max(NodeCode) + 1` |
| `ModifyTime` | 保留旧值或写入当前时间（见 §9 待确认项） |
| 其余字段 | 从旧节点原样复制 |

### 5.1 父链补建

若待补建节点的父节点在新文件中**也不存在**，需沿 `Path` **逐级向上检查并补建整条父链**：

```
补建 config/a/b/c 时：
  1. 检查 config/a 是否存在 → 不存在则先补建（生成 UUID）
  2. 检查 config/a/b 是否存在 → 不存在则补建，ParentUUID 指向 config/a 的新 UUID
  3. 补建 config/a/b/c，ParentUUID 指向 config/a/b 的新 UUID
```

**必须自上而下（从根到叶）依次生成**，确保子节点能取到父节点已生成的 UUID。

### 5.2 Tags 数组合并

若补建节点携带的 `TagName` 在新文件的 `Tags` 数组中不存在，需将其追加到 `Tags` 数组中，保证文件自洽。

---

## 6. 输出前一致性自检

**这是全自动模式的安全网。** 由于不做人工干预，必须在导出前强制校验输出文件的结构正确性。

### 6.1 硬性检查项（不通过则拒绝导出）

对应 §2.3 的四条不变量：

1. **无孤儿节点**：每个非根节点的 `ParentUUID` 都能在文件中找到
2. **Path 自洽**：每个节点 `Path` == `父节点.Path + "/" + SystemName`（根节点为 `config/SystemName`）
3. **Level 自洽**：每个节点 `Level` == `父节点.Level + 1`（根节点为 1）
4. **唯一性**：`UUID`、`Id`、`Path` 三者各自全局唯一

### 6.2 软性检查项（记录告警，不阻断）

- 每个旧通参的叶子节点，都能在输出中找到对应参数（**迁移完整性**）
- 分组节点（`NodeType == 1`）的 `ParameterValue` 为空
- 每个叶子节点的值都能通过其自身的校验规则

### 6.3 失败处理

硬性检查不通过时：

- **不生成输出文件**
- 弹出错误提示并输出诊断报告，列出具体违反了哪条不变量、涉及哪些节点

---

## 6A. 输出文件序列化格式规格

**要求**：输出 JSON 必须与新通参 JSON 的文件格式完全一致，在不破坏数据格式的前提下更新参数。

**已实测验证**：用 `json.loads` 读入样本文件后再 `json.dumps` 写出，**1,801,926 字节与原文件完全一致（字节级零差异）**。因此格式一致性可被严格保证，而非依赖肉眼比对。

### 6A.1 格式规格（实测）

| 特征 | 规格 |
| --- | --- |
| 编码 | UTF-8，**无 BOM** |
| 换行符 | **LF**（`\n`），无 CRLF |
| 缩进 | **2 空格** |
| 非 ASCII 字符 | **不转义**，直接输出汉字（非 `\uXXXX`） |
| 分隔符 | 标准 `", "` 与 `": "`（非紧凑模式） |
| 文件末尾 | **无换行符** |
| 节点字段顺序 | 全部 1878 个节点**完全一致**，共 27 字段，仅 1 种顺序 |

标准字段顺序：

```
UUID, ParentUUID, NodeType, Level, NodeCode, NodeName, SystemName,
DataTypeId, ParameterUnit, ValueRange, CheckType, VerifyExpression,
ParameterValue, ParameterDefaultValue, ParameterDestriottion,
IsEdit, IsShow, Path, TagId, Tag, TagName, ModifyTime,
IsSearchExportNode, RangeExplain, LstEnumExplain, ChildNodes, Id
```

对应实现：`json.dumps(data, ensure_ascii=False, indent=2)`，写文件时 `encoding='utf-8'`、`newline='\n'`。

### 6A.2 实现约束

**① 字段顺序靠「不重建字典」保证**

Python 3.7+ 的 dict 保持插入顺序，`json.loads` 读入的顺序天然即原文顺序。**只做 in-place 赋值，不重建 dict，顺序即不会变。**

- ❌ 禁止：手写字面量字典构造节点 —— 字段顺序必然出错
- ✅ 正确：补建节点（P4）时以**旧节点 dict 深拷贝**为模板，再逐字段覆盖

**② 禁止经由 pandas / ORM 中转节点数据**

一旦将节点塞入 DataFrame 或拆解为对象属性再重组，字段顺序、`null` 与 `""` 的区分、数字与字符串的区分都会被破坏。

`core/models.py` 的 `ParamNode` 应**持有原始 dict 的引用**，而非把字段拆成独立属性后重新组装。

**③ 类型不得漂移**

| 字段 | 正确类型 | 风险 |
| --- | --- | --- |
| `ParameterValue` | 恒为 **string**（即使内容是 `0.1`） | 被转为 float 后写回 |
| `ParameterDefaultValue` | string 或 **`null`**（分组节点） | `null` 与 `""` 混用 |
| `TagId` | **int** 或 `null` | 与 string 类型的 `Tag`/`TagName` 混淆 |
| `HashCode` | **int** | 被转为 string |
| `VerifyExpression` | **被转义的 JSON 字符串** | 解析后将解析结果写回，破坏原格式 |

**④ 浮点值全程按字符串搬运**

`0.304000` 这类带尾随零的值，解析为 float 再写回会变成 `0.304`——数值相等但格式改变。

由于 `ParameterValue` 本身即字符串，**全程按字符串处理，永不做 float 转换回写**。校验时可临时转 float 判断，但结果不回写。

### 6A.3 字节级回归测试【最高优先级】

> **新旧传入同一份文件时，输出文件必须与输入文件字节完全一致。**

此测试已验证技术上完全可达，可一次性覆盖：字段顺序错乱、类型漂移、编码问题、缩进问题、`null`/`""` 混淆等全部格式类缺陷。

**建议作为项目第一个实现的测试用例。**

---

## 7. 技术方案

### 7.1 技术栈

**Python 3.10+ / PySide6 / PyInstaller 打包为单 exe**

选型理由：

- 工具本质是「数据转换 + 对比表格展示」，Python 在 JSON 处理、树结构操作、报告生成上表达力最强
- `QTreeView` 原生支持树形对比展示
- `pandas` + `openpyxl` 可低成本产出 Excel 报告
- PyInstaller 单文件打包，现场工程师双击即用，无需安装运行时

### 7.2 架构原则

**核心转换逻辑与 UI 完全解耦。** `core/` 目录下不得出现任何 `PySide6` 的 import。

带来的实际收益：

- 转换逻辑可编写自动化测试（用真实新旧文件对跑并断言结果）
- UI 问题不影响核心正确性
- 可通过命令行接入自动化流水线

**开发顺序建议**：先完成 `core/` 全部逻辑并通过测试，再开发 UI。顺序颠倒会极难调试。

### 7.3 目录结构

```
ContainerTemplateManager/
├── core/                      # 纯逻辑，零 UI 依赖
│   ├── models.py              # ParamNode / ParamTree 数据模型
│   ├── loader.py              # JSON 解析、建树、内部一致性校验
│   ├── matcher.py             # Path 精确匹配 + 重命名嗅探
│   ├── policy.py              # P0–P4 决策引擎【核心，需重点 review】
│   ├── validator.py           # DataTypeId / CheckType / VerifyExpression 校验
│   ├── builder.py             # 输出树构建：UUID / Id / Level / NodeCode 重建
│   ├── hashcode.py            # 可插拔 HashCode 策略
│   └── report.py              # 报告数据结构与导出
├── config/
│   ├── force_migrate.yaml     # 强制迁移清单（外部可更新，无需重新打包）
│   └── operators.yaml         # VerifyExpression 运算符映射表
├── ui/
│   ├── main_window.py         # 主界面
│   └── compare_view.py        # 人工选择模式对比界面
├── cli.py                     # 命令行入口
└── tests/                     # 回归测试
```

### 7.4 强制迁移清单配置

**必须为软件外部的配置文件**，随程序发布但可单独更新——否则每次新增标定参数都需重新编译打包。

**格式要求**：

| 列 | 必填 | 说明 |
| --- | --- | --- |
| `path` | 是 | 匹配键，使用 `Path`。**支持 `**` 通配符**，如 `config/static/**` 表示整棵子树 |
| `new_path` | 否 | 新版本路径。留空表示路径未变；填写则作为**显式映射**，覆盖自动匹配 |
| `category` | 是 | 分类：`calibration`（标定）/ `static`（静态）/ `other` |
| `comment` | 否 | 备注说明 |

**为什么必须支持通配符**：`config/static` 子树有 128 个叶子节点，逐条列举维护成本过高，且新版本增删参数后清单会立即过期。

示例：

```yaml
- path: "config/static/**"
  category: static
  comment: 静态参数整棵子树强制迁移

- path: "config/calibration/**"
  category: calibration
  comment: 标定参数整棵子树强制迁移

- path: "config/perception/Sensor/grounding_fork_height"
  category: calibration
  comment: 传感器触地临界点的货叉高度，无 Tag 但属标定值
```

> **当前状态**：清单内容由业务方梳理提供，本文档仅规定格式。实现时先以空清单跑通流程。

### 7.5 运算符映射表配置

将 §2.5 的运算符编码表实现为配置文件，便于后续补充未验证的编码：

```yaml
min_operator:
  2: "ge"        # Min ≤ 值
max_operator:
  2: "le"        # 值 ≤ Max
  3: "gt"        # 值 > Max（注意：语义为下界）
  4: "ge"        # 值 ≥ Max（注意：语义为下界）
unknown_behavior: "skip_with_warning"
```

### 7.6 HashCode 处理

**已确认设备导入时不校验 HashCode。**

实现为**策略模式**，默认使用 `PassthroughStrategy`（沿用新模板的值）。保留接口以便未来需求变化：

- `PassthroughStrategy` —— 沿用新模板的值【默认】
- `NullStrategy` —— 置空或置 0（测试用）
- `ComputeStrategy` —— 预留占位

调用方仅依赖接口，未来若需真实计算，只改此单个文件。

---

## 8. 功能与界面设计

### 8.1 主界面

界面保持简洁，包含：

- **旧通参导入区**：文件选择按钮 + 路径显示 + 加载状态（节点数、根节点数）
- **新通参导入区**：同上
- **功能按钮**：
  - `一键通参转换`
  - `人工选择通参转换`
- 两个文件均加载成功前，功能按钮置灰

**加载时即执行**内部一致性校验（§2.3），文件本身结构有问题应立即提示，不进入后续流程。

### 8.2 一键通参转换

**行为**：全自动执行，无任何人工干预。

**流程**：

```
加载两文件
  → 执行匹配（§3）
  → 执行取值决策（§4）
  → 执行校验与兜底（§4.4）
  → 构建输出树（§5）
  → 一致性自检（§6）
  → 弹出保存对话框，导出 JSON
  → 同时导出转换报告
  → 展示结果摘要
```

**结果摘要**至少包含：各决策分类（P0~P4）的参数数量、告警数量、需人工复核项数量。

### 8.3 人工选择通参转换

**行为**：进入新旧通参对比界面，用户可逐条查看并调整决策后再导出。

**界面要求**：

- 树形或表格展示全部参数，左右对照显示旧值与新值
- 每行展示：`Path`、`NodeName`、旧当前值、旧默认值、新默认值、**系统建议值**、决策分类
- 按决策分类**支持筛选**（如只看 P0、只看告警项、只看有差异项）
- 用户可**逐条覆盖**系统建议值
- 默认选中系统建议值 —— 即用户不做任何操作时，结果与一键转换完全一致
- 确认后执行与一键模式相同的后续流程（校验 → 构建 → 自检 → 导出）

**设计理由**：实测样本中仅 41/1696 个参数被定制过，需人工关注的行数很少，全量展示 + 筛选完全可用。

### 8.4 转换报告

**格式**：Excel（便于业务方留档与传阅）或 HTML。建议 Excel。

**内容分区**（按重要性排序）：

1. **需人工复核项**【置顶】—— 校验失败被回退的参数，P0 类单独成组
2. **单位/类型变更告警** —— `ParameterUnit`、`DataTypeId` 发生变化的参数
3. **强制迁移明细（P0）** —— 标定与静态参数的迁移记录
4. **人工微调迁移明细（P1）**
5. **补建节点明细（P4）** —— 含新生成的 UUID / Id
6. **重命名嗅探结果** —— 含低置信匹配与多候选情况
7. **新版本新增参数（P3）**
8. **统计摘要**

每条记录至少包含：`Path`、`NodeName`、旧当前值、旧默认值、新默认值、最终采用值、决策依据、告警说明。

---

## 9. 待确认项

| 编号 | 事项 | 影响 | 状态 |
| --- | --- | --- | --- |
| 1 | 强制迁移清单具体内容 | 决定 P0 覆盖范围 | 业务方梳理中，格式已定（§7.4） |
| 2 | 补建节点的 `ModifyTime` 取值：保留旧值 or 写入当前时间 | 影响可追溯性 | 待确认 |
| 3 | `MinOperator` 编码 1/3/4 的语义 | 影响校验准确性 | 样本中未出现，已按配置表 + 告警降级处理 |
| 4 | `IsSearchExportNode` / `RangeExplain` 字段用途 | 暂无影响，原样保留 | 待确认 |
| 5 | 新版本删除的参数是否应补建 | 当前按 P4 一律补建 | 已确认按需求执行，风险由重命名嗅探缓解 |

---

## 10. 测试要求

### 10.1 core 层单元测试（必须先于 UI 完成）

| 测试项 | 说明 |
| --- | --- |
| 结构不变量校验 | 用样本文件验证 §2.3 四条不变量全部通过 |
| Path 唯一性 | 验证样本 1878 节点 Path 无重复 |
| 运算符解析 | 用 `ValueRange` 交叉验证 `VerifyExpression` 解析结果 |
| 异常表达式容错 | 验证 53 个 `CheckType=0` 带文本表达式的节点不抛异常 |
| P0~P4 决策 | 为每条优先级构造用例，验证命中顺序正确 |
| 补建节点 | 验证 UUID/Id/Level/NodeCode 生成正确，父链完整 |
| 输出自检 | 验证输出文件满足全部硬性检查 |

### 10.2 端到端测试

- **同文件转换【最高优先级】**：新旧传入同一份文件，输出必须与输入**字节完全一致**。已实测验证可达（见 §6A.3），是最强的回归测试，建议第一个实现
- **格式保真**：输出文件的编码、换行符、缩进、字段顺序、类型均符合 §6A.1 规格
- **完整迁移**：验证旧通参的每个叶子节点都能在输出中找到归宿
- **41 个定制值**：验证样本中全部定制值正确迁移
